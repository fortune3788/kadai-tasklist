<%# if logged_in? %>
  <h1>タスク一覧</h1>

  <ul>
    <% @tasks.each do |task| %>
      <li><%= link_to task.id, task %> : <%= task.status %> &spades; <%= task.content %></li>
    <% end %>
  </ul>

  <%= link_to '新規タスクの投稿', new_task_path %>
<%# else %>
  <!--
  <div>
    <div>
      <h1>Tasklist app</h1>
      <%#= link_to '新規登録する', signup_path %>
    </div>
  </div>
  --!>
<%# end %>